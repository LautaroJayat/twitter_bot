const _makeRequest = require('./makeRequest')
const tokenCredentials = require('./tokenCredentials')
const URLencoder = require('./URLencoder')
const readFile = require('./readFile')

module.exports = {
    Terminalcolors,
    _makeRequest,
    tokenCredentials,
    URLencoder,
    readFile
}